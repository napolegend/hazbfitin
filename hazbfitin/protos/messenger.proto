syntax = "proto3";

message Message {
  string nickname = 1;
  bytes data = 2;
}
message Authorize {
  string nickname = 1;
  int32 mark = 2;
}


message Empty {}

service Chatting {
  rpc ChatStream(Authorize) returns (stream Message);
  rpc SendMessage(Message) returns (Empty);
}


